#include <iostream>
using namespace std;

int main(){
    float x;
    float y;
    float q;
    cout << "Enter the number(x): ";
    cin >> x;
    cout << "Enter the number(y): ";
    cin >> y;
    cout << "Enter the number(q): ";
    cin >> q;
    cout << "Output number(x+y+q): " << (x+y+q) << endl;
    cout << "Output number(x*y*q): " << (x*y*q) << endl;
    return 0;
}